name: kominfo_dashboard_test
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  email_validator: ^2.1.17
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0
  gap: ^3.0.1
  local_auth: ^2.1.7
  shared_preferences: ^2.2.2
  supabase_auth_ui: ^0.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
